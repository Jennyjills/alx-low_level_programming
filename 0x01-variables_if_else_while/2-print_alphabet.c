#include <stdio.h>

/**
* main - prints the alphabet in lowercase, followed by a new line.
*/
int main(void)
{
   int ch;
   for(ch=97;ch<=122;ch++);
    for ("ch = 'a'; ch <= 'z'; ++c);
    {
        putchar(10);
    }
    putchar("\n")
    
    return (0);
}  