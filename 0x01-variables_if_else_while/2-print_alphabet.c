#include <stdio.h>
/**
* main - prints the alphabet in lowercase, followed by a new line.
*/
int main(void)
{
    char ch = 'a';
    for ("ch = 'a'; ch <= 'z'; ++c);
        putchar("%c\n", ch);
    return (0);
}  