#include <stdio.h>

/**
 * main -> prints exactly the alphabets in lowercase
 * Return : always 0
 */

int main(void)
{
	int n;

	for (n = 0; n<10;n++)
	{
		printf("%d\n", n);
		putchar(ch);
	}

return (0);
}
